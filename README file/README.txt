"e, this should be ASCII text that includes information pertinent to compiling
and running your project" - To run the build version open the "RubeGoldbergMachineBuild" application file.

All other relevant files are in the same 'Unity build' folder.
